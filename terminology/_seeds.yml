version: 2

seeds:
  - name: terminology__acute_diagnosis_ccs
    config:
      database: "{{ var('terminology_database') }}"
      schema: "{{ var('terminology_schema') }} "
      alias: acute_diagnosis_ccs
      tags:
        - terminology
        - readmissions
      column_types:
        ccs_diagnosis_category : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
        description : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
  - name: terminology__acute_diagnosis_icd_10_cm
    config:
      database: "{{ var('terminology_database') }}"
      schema: "{{ var('terminology_schema') }} "
      alias: acute_diagnosis_icd_10_cm
      tags:
        - terminology
        - readmissions
      column_types:
        icd_10_cm : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
        description : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
  - name: terminology__admit_source
    config:
      database: "{{ var('terminology_database') }}"
      schema: "{{ var('terminology_schema') }} "
      alias: admit_source
      tags:
        - terminology
        - claims_preprocessing
      column_types:
        admit_source_code : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
        admit_source_description : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
        newborn_description : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
  - name: terminology__admit_type
    config:
      database: "{{ var('terminology_database') }}"
      schema: "{{ var('terminology_schema') }} "
      alias: admit_type
      tags:
        - terminology
        - claims_preprocessing
      column_types:
        code : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
        description : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
  - name: terminology__always_planned_ccs_diagnosis_category
    config:
      database: "{{ var('terminology_database') }}"
      schema: "{{ var('terminology_schema') }} "
      alias: always_planned_ccs_diagnosis_category
      tags:
        - terminology
        - readmissions
      column_types:
        ccs_diagnosis_category : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
        description : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
  - name: terminology__always_planned_ccs_procedure_category
    config:
      database: "{{ var('terminology_database') }}"
      schema: "{{ var('terminology_schema') }} "
      alias: always_planned_ccs_procedure_category
      tags:
        - terminology
        - readmissions
      column_types:
        ccs_procedure_category : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
        description : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
  - name: terminology__bill_type
    config:
      database: "{{ var('terminology_database') }}"
      schema: "{{ var('terminology_schema') }} "
      alias: bill_type
      tags:
        - terminology
        - data_profiling
      column_types:
        bill_type_code : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
        bill_type_description : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
        deprecated : int
        deprecated_date : date
  - name: terminology__chronic_conditions
    config:
      database: "{{ var('terminology_database') }}"
      schema: "{{ var('terminology_schema') }} "
      alias: chronic_conditions
      tags:
        - terminology
        - chronic_conditions
      column_types:
        condition_id : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
        condition : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
        condition_column_name : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
        chronic_condition_type : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
        condition_category : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
        additional_logic : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar(1000) {%- endif -%}
        claims_qualification : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar(1000) {%- endif -%}
        inclusion_type : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
        code_system : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
        code : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
  - name: terminology__claim_type
    config:
      database: "{{ var('terminology_database') }}"
      schema: "{{ var('terminology_schema') }} "
      alias: claim_type
      tags:
        - terminology
        - data_profiling
      column_types:
        claim_type_code : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
        claim_type_description : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
  - name: terminology__code_type
    config:
      database: "{{ var('terminology_database') }}"
      schema: "{{ var('terminology_schema') }} "
      alias: code_type
      tags: terminology
      column_types:
        description : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
  - name: terminology__discharge_disposition
    config:
      database: "{{ var('terminology_database') }}"
      schema: "{{ var('terminology_schema') }} "
      alias: discharge_disposition
      tags:
        - terminology
        - claims_preprocessing
        - readmissions
        - data_profiling
      column_types:
        discharge_disposition_code : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
        discharge_disposition_description : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
  - name: terminology__encounter_type
    config:
      database: "{{ var('terminology_database') }}"
      schema: "{{ var('terminology_schema') }} "
      alias: encounter_type
      tags:
        - terminology
        - claims_preprocessing
      column_types:
        description : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
  - name: terminology__ethnicity
    config:
      database: "{{ var('terminology_database') }}"
      schema: "{{ var('terminology_schema') }} "
      alias: ethnicity
      tags: terminology
      column_types:
        code : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
        description : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
  - name: terminology__exclusion_ccs_diagnosis_category
    config:
      database: "{{ var('terminology_database') }}"
      schema: "{{ var('terminology_schema') }} "
      alias: exclusion_ccs_diagnosis_category
      tags: terminology
      column_types:
        ccs_diagnosis_category : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
        description : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
        exclusion_category : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
  - name: terminology__fips_county
    config:
      database: "{{ var('terminology_database') }}"
      schema: "{{ var('terminology_schema') }} "
      alias: fips_county
      tags: terminology
      column_types:
        fips_code : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
        county : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
        state : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
  - name: terminology__fips_state
    config:
      database: "{{ var('terminology_database') }}"
      schema: "{{ var('terminology_schema') }} "
      alias: fips_state
      tags: terminology
      column_types:
        fips_code : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
        abbreviation : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
        description : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
  - name: terminology__gender
    config:
      database: "{{ var('terminology_database') }}"
      schema: "{{ var('terminology_schema') }} "
      alias: gender
      tags:
        - terminology
        - readmissions
        - data_profiling
      column_types:
        description : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
  - name: terminology__hcpcs_level_2
    config:
      database: "{{ var('terminology_database') }}"
      schema: "{{ var('terminology_schema') }} "
      alias: hcpcs_level_2
      tags: terminology
      column_types:
        hcpcs : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
        seqnum : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
        recid : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
        long_description : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar(2000) {%- endif -%}
        short_description : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
  - name: terminology__icd_10_cm
    config:
      database: "{{ var('terminology_database') }}"
      schema: "{{ var('terminology_schema') }} "
      alias: icd_10_cm
      tags:
        - terminology
        - claims_preprocessing
        - readmissions
        - data_profiling
      column_types:
        icd_10_cm : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
        valid_flag : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
        short_description : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
        long_description : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
  - name: terminology__icd_10_cm_to_ccs
    config:
      database: "{{ var('terminology_database') }}"
      schema: "{{ var('terminology_schema') }} "
      alias: icd_10_cm_to_ccs
      tags:
        - terminology
        - readmissions
      column_types:
        icd_10_cm : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
        description : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
        ccs_diagnosis_category : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
        ccs_description : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
  - name: terminology__icd_10_pcs
    config:
      database: "{{ var('terminology_database') }}"
      schema: "{{ var('terminology_schema') }} "
      alias: icd_10_pcs
      tags:
        - terminology
        - claims_preprocessing
        - readmissions
      column_types:
        icd_10_pcs : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
        valid_flag : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
        short_description : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
        long_description : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
  - name: terminology__icd_10_pcs_to_ccs
    config:
      database: "{{ var('terminology_database') }}"
      schema: "{{ var('terminology_schema') }} "
      alias: icd_10_pcs_to_ccs
      tags:
        - terminology
        - readmissions
      column_types:
        icd_10_pcs : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
        description : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
        ccs_procedure_category : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
        ccs_description : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
  - name: terminology__mdc
    config:
      database: "{{ var('terminology_database') }}"
      schema: "{{ var('terminology_schema') }} "
      alias: mdc
      tags: terminology
      column_types:
        mdc_code : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
        mcd_description : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
  - name: terminology__medicare_dual_eligibility
    config:
      database: "{{ var('terminology_database') }}"
      schema: "{{ var('terminology_schema') }} "
      alias: medicare_dual_eligibility
      tags: terminology
      column_types:
        dual_status_code : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
        dual_status_description : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
  - name: terminology__medicare_state_fips
    config:
      database: "{{ var('terminology_database') }}"
      schema: "{{ var('terminology_schema') }} "
      alias: medicare_state_fips
      tags: terminology
      column_types:
        fips_code : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
        state : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
  - name: terminology__medicare_status
    config:
      database: "{{ var('terminology_database') }}"
      schema: "{{ var('terminology_schema') }} "
      alias: medicare_status
      tags: terminology
      column_types:
        medicare_status_code : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
        medicare_status_description : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
  - name: terminology__ms_drg
    config:
      database: "{{ var('terminology_database') }}"
      schema: "{{ var('terminology_schema') }} "
      alias: ms_drg
      tags:
        - terminology
        - readmissions
        - data_profiling
      column_types:
        ms_drg_code : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
        mdc : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
        medical_surgical : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
        ms_drg_description : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
        deprecated: integer
        deprecated_date: date
  - name: terminology__payer_type
    config:
      database: "{{ var('terminology_database') }}"
      schema: "{{ var('terminology_schema') }} "
      alias: payer_type
      tags: terminology
      column_types:
        payer_type : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
  - name: terminology__place_of_service
    config:
      database: "{{ var('terminology_database') }}"
      schema: "{{ var('terminology_schema') }} "
      alias: place_of_service
      tags:
        - terminology
        - claims_preprocessing
        - data_profiling
      column_types:
        place_of_service_code : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
        place_of_service_description : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
  - name: terminology__potentially_planned_ccs_procedure_category
    config:
      database: "{{ var('terminology_database') }}"
      schema: "{{ var('terminology_schema') }} "
      alias: potentially_planned_ccs_procedure_category
      tags:
        - terminology
        - readmissions
      column_types:
        ccs_procedure_category : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
        description : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
  - name: terminology__potentially_planned_icd_10_pcs
    config:
      database: "{{ var('terminology_database') }}"
      schema: "{{ var('terminology_schema') }} "
      alias: potentially_planned_icd_10_pcs
      tags:
        - terminology
        - readmissions
      column_types:
        icd_10_pcs : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
        description : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
  - name: terminology__present_on_admission
    config:
      database: "{{ var('terminology_database') }}"
      schema: "{{ var('terminology_schema') }} "
      alias: present_on_admission
      tags:
        - terminology
        - data_profiling
      column_types:
        present_on_admit_code : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
        description : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
  - name: terminology__race
    config:
      database: "{{ var('terminology_database') }}"
      schema: "{{ var('terminology_schema') }} "
      alias: race
      tags: terminology
      column_types:
        race_code : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
        race_description : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
  - name: terminology__revenue_center
    config:
      database: "{{ var('terminology_database') }}"
      schema: "{{ var('terminology_schema') }} "
      alias: revenue_center
      tags:
        - terminology
        - claims_preprocessing
        - data_profiling
      column_types:
        revenue_center_code : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
        description : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
  - name: terminology__specialty_cohort
    config:
      database: "{{ var('terminology_database') }}"
      schema: "{{ var('terminology_schema') }} "
      alias: specialty_cohort
      tags:
        - terminology
        - readmissions
      column_types:
        ccs : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
        description : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
        specialty_cohort : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
        procedure_or_diagnosis : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
  - name: terminology__surgery_gynecology_cohort
    config:
      database: "{{ var('terminology_database') }}"
      schema: "{{ var('terminology_schema') }} "
      alias: surgery_gynecology_cohort
      tags:
        - terminology
        - readmissions
      column_types:
        icd_10_pcs : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
        description : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
        ccs_code_and_description : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
        specialty_cohort : |
          {%- if target.type == "bigquery" -%} string {%- else -%} varchar {%- endif -%}
